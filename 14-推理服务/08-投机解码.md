---
tags:
  - ai工程/推理服务
stage: 8
status: todo
aliases:
  - speculative decoding
  - EAGLE
  - draft model
---
让一个小模型先猜几个字，大模型一次性验证。猜对了白赚，猜错了丢掉重来——听起来像稳赚，实际上并发一高，这笔买卖就越来越不划算。

## 一句话

投机解码用**闲置的算力**换 decode 速度，所以收益随算力的空闲程度走——batch 一大，算力本来就吃满了，加速比会从几倍衰减到一点几倍。

## 原理

### 它为什么能成立

decode 阶段是 memory-bound 的：每生成一个 token 要把全部权重搬一遍，算力大量空转，见 [[01-Prefill vs Decode]] 和 [[02-算力经常用不满：两种瓶颈]]。

关键洞察是：**验证 5 个 token 和验证 1 个 token，搬的权重一样多。** 因为都只需要读一遍权重，而多算几个位置对 GPU 来说几乎免费。

于是：

1. 一个便宜的**草稿模型**快速猜出接下来 $k$ 个 token
2. 大模型**一次前向**同时验证这 $k$ 个位置
3. 从头开始比对：猜对的直接采纳，第一个猜错的位置纠正过来，后面的丢掉

数学上可以做到**输出分布和不用投机时完全一致**——这不是近似加速，是无损的。

### 草稿从哪来

| 做法 | 怎么产生草稿 | 特点 |
| --- | --- | --- |
| 独立小模型 | 跑一个同族的小模型 | 通用，但要额外显存和一次完整前向 |
| **EAGLE 系** | 用目标模型的中间层特征，配一个极轻的头 | 接受率高、开销小，目前工程上的主流 |
| n-gram / 提示词查找 | 直接从上下文里找重复片段 | 几乎零开销，只在有大量重复时有效 |

最后一种在特定场景出奇地好用：让模型改写一段给定文本、或者输出大量引用原文的内容时，重复率极高。

### 收益取决于两个数

**接受率**——草稿平均能被采纳几个。它取决于草稿模型和目标模型有多像，**和硬件无关**——换代 GPU 不会改变接受率。

**batch 大小**——这是那个反直觉的部分。

> [!warning] batch 越大，收益越薄
> [EAGLE-3](https://arxiv.org/abs/2503.01840)（Yuhui Li 等，2025 年 3 月）报告的加速比**最高 6.5 倍**——那是单请求、低 batch 的数。同一篇论文里，在 SGLang 上 **batch 64 时的吞吐提升是 1.38 倍**。
>
> 从 6.5 到 1.38，这个衰减方向就是全部要点。原因回到那条主线：投机解码**花的是算力，赚的是带宽**。低 batch 时算力大量空转，多算几个位置几乎不要钱；batch 大到算力本来就吃紧时，草稿和验证的额外计算变成真实开销，收益被自己吃掉一大半。
>
> **所以它主要是「低并发时的延迟优化」，顺带在中等并发下还有一点吞吐收益。** 别拿论文标题上那个最大加速比去做容量规划。

注意 1.38 倍不是零——**「高并发就该关掉」这个判断需要你自己测过才成立**，别直接照抄。真实的做法是扫一遍 batch size 画出加速比曲线，找到收益跌破「值得为它多担一份运维复杂度」的那个点，再据此写动态开关的阈值。那个点在哪儿取决于你的模型、草稿头和流量分布，不存在通用答案。

### 另一个坑：分布外就退化

投机解码在受控环境里表现很好，换到真实流量就可能打折。EAGLE 团队把这个现象归因为 **attention drift**——草稿模型在遇到不同的 chat template、长上下文输入、或者没见过的 system prompt 时，接受率会明显下降。

EAGLE 3.1（2026 年 5 月，EAGLE、vLLM、TorchSpec 三方合作）通过在每个目标隐状态后加 FC 归一化、并把归一化后的隐状态喂进下一步来缓解这一点。

工程上的含义：**必须用你自己的真实流量测接受率**，别信论文里的数字。你的 system prompt 和上下文长度和它们不一样。

### 什么时候值得上

**值得**：交互式场景、低到中等并发、对首字后的流畅度敏感、有闲置算力。

**不值得**：高并发批处理（算力已经吃满）、显存本来就紧张（草稿模型也要占显存）、请求分布特别杂（接受率上不去）。

**先做别的**：连续批处理、前缀缓存、量化这三样的收益更确定也更普适，做完再考虑投机解码。它属于「前面都做完了还想再快一点」的那一档。

## 动手做

- [ ] 接一个 EAGLE 类的草稿模型，用**自己的真实流量**测接受率，别用示例数据
- [ ] 扫 batch size 从 1 到 128，画出加速比曲线，找到收益跌破「值得为它多担一份复杂度」的那个点
- [ ] 换一批分布外的 prompt（不同 system prompt、超长上下文）再测接受率，看掉多少
- [ ] 对比开与不开时的显存占用，确认草稿模型的额外开销能接受
- [ ] 验证输出分布是否真的无损：同一 prompt 固定种子跑两版，比对结果
- [ ] 根据自己测出的曲线写一条运维规则：并发超过多少就关掉，阈值要有实测支撑

## 算学会了

- [ ] 能解释为什么投机解码花的是算力、赚的是带宽
- [ ] 知道加速比随 batch 增大而衰减，并能说出自己系统在哪个 batch 上不再划算
- [ ] 能用自己测的曲线回答「我们高峰期该不该开这个」
- [ ] 知道接受率必须用自己的流量测，论文数字不可移植
- [ ] 会把它排在连续批处理、前缀缓存、量化之后再考虑

## 坑

**拿论文标题上那个最大加速比做容量规划。** 6.5 倍是单请求的数，你的生产 batch 上拿不到。不实测就全局开启，高峰期的收益远低于预期，甚至可能被额外开销吃穿。

**信论文里的接受率。** 你的 chat template、system prompt、上下文长度都不一样，实测通常更低。

**忘了草稿模型也占显存。** 在显存本来就紧张的配置上，这部分可能挤掉 KV cache，反而降低并发。

**当成吞吐优化。** 它是低并发下的延迟优化，两者的适用场景相反。

**跳过前面几项直接上它。** 连续批处理和前缀缓存的收益更确定，先做那些。

## 关联
- [[01-Prefill vs Decode]]
- [[07-Continuous batching]]
- [[02-算力经常用不满：两种瓶颈]]
- [[10-吞吐-延迟帕累托前沿]]
- [[09-量化]]

## 参考

| 年份 | 工作 | 人与机构 | 在本篇的位置 |
| --- | --- | --- | --- |
| 2022 | [Fast Inference from Transformers via Speculative Decoding](https://arxiv.org/abs/2211.17192) | Yaniv Leviathan、Matan Kalman、Yossi Matias，Google Research | 「输出分布完全一致」这个无损性质的证明——本篇说它「不是近似加速」的依据 |
| 2024 | [EAGLE: Speculative Sampling Requires Rethinking Feature Uncertainty](https://arxiv.org/abs/2401.15077) | Yuhui Li、Fangyun Wei、Chao Zhang、Hongyang Zhang，北京大学、微软亚洲研究院与滑铁卢大学 | 「用目标模型中间层特征配一个轻量头」这条路线的起点，即上面表格里的 EAGLE 系 |
| 2025 | [EAGLE-3: Scaling up Inference Acceleration of Large Language Models via Training-Time Test](https://arxiv.org/abs/2503.01840) | 同上四人 | 那个 warning callout 里的两个数：最高 6.5 倍加速比，以及 SGLang 上 batch 64 时 1.38 倍吞吐 |
| 2026 | [EAGLE 3.1: Advancing Speculative Decoding](https://vllm.ai/blog/2026-05-26-eagle-3-1) | EAGLE、vLLM、TorchSpec 三方 | attention drift 现象与 FC 归一化的修法，本篇分布外退化那段 |

**这条线三年走完的路很清楚**：2022 年证明它无损，2024 年把草稿模型做便宜，2025 年把接受率做高，2026 年开始修真实流量下的退化。**每一步解决的都是上一步暴露出来的瓶颈**——所以看到新的加速比数字时，先问它是在哪一档 batch、哪种流量分布上测的。
