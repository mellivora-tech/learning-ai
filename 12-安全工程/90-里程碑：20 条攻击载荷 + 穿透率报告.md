---
tags:
  - ai工程/安全
  - 里程碑
stage: 6
status: todo
---
## 交付物

一份**自己写的**攻击载荷集，加一份修复前后的对比报告。

- **20 条种子载荷**，覆盖八个类别，且是针对你自己系统结构写的（不是模型生成的通用模板）
- 每条载荷在**多个入口**各试一遍：用户输入、检索文档、工具返回、上一轮输出
- **分级穿透率报告**：critical / high / medium 各自的穿透条数与比例
- **修复前后对比**：改了什么、穿透率从多少降到多少
- critical 那档接进 CI，零容忍
- 五类多租户隔离测试全部通过

## 做法

统计与判定口径有起步脚本，五个函数留空，填一个过一个：

```bash
python "_练习/12-穿透率报告.py"
```

被测系统是脚本内置的替身，不用真调模型。**练的是口径，因为穿透率算错会得出一份好看的报告**——按条数平均会让 critical 被 medium 稀释，用文本匹配当判据会漏掉模型静默执行的情况。

| 关 | 要填 | 谁当裁判 |
| --- | --- | --- |
| 1 | `build_matrix` | 每条载荷必须在每个入口各出现一次 |
| 2 | `is_penetrated` | 「拒绝但复述了动作名」不算穿透；「一声不吭地做了」必须算 |
| 3 | `penetration_by_level` | 构造 critical 全穿而总体只有 10% 的数据，分级后必须看得出来 |
| 4 | `gate` | critical 零容忍——穿透一条也要拦；用例不足同样要拦 |
| 5 | `isolation_report` | **没测过的面等同于没通过**，漏测不能静默放行 |

五关都过之后脚本会打印修复前后对比，并按入口拆开——替身系统的形态是「用户输入 0 穿透、其余三个入口全穿」，而总体数字只显示 75%。**这正是本里程碑要求逐入口测试的理由。**

然后是换成自己系统的那一半：

1. 列出系统里所有不可信内容的入口，见 [[01-间接提示注入与 20 条攻击载荷]]
2. 八类各写两三条种子载荷——**自己写**，模型只能帮你扩写变体
3. 每条埋在不同入口各跑一遍，记录穿透情况
4. 按 critical / high / medium 分级统计
5. 按纵深防御的优先级修：先收工具权限、给不可逆操作加人确认，再做结构化分隔，见 [[02-纵深防御]]
6. 高风险场景实施隔离，哪怕只是简化版的按阶段收权限，见 [[03-隔离模式（dual-LLM）]]
7. 把权限过滤从生成后改到检索时，见 [[04-权限过滤时机]]
8. 审计所有 cache key，做跨租户红队，见 [[05-缓存安全与跨租户命中]]
9. 跑五类多租户隔离测试，见 [[06-多租户隔离]]
10. 重跑全部载荷，出对比报告

## 结果与数据

| 类别 | 载荷数 | 修复前穿透 | 修复后穿透 |
| --- | --- | --- | --- |
| 指令覆盖 | | | |
| 角色劫持 | | | |
| 信息外泄 | | | |
| 工具滥用 | | | |
| 越权检索 | | | |
| 外泄通道（markdown 图片等） | | | |
| 编码混淆 | | | |
| 多轮铺垫 | | | |

| 入口 | 穿透率 |
| --- | --- |
| 用户输入 | |
| 检索文档 | |
| 工具返回 | |
| 上一轮输出 | |

| 隔离测试 | 结果 |
| --- | --- |
| 跨租户检索 | |
| 跨租户缓存命中 | |
| 配额生效（噪音邻居） | |
| 日志隔离 | |
| 租户删除彻底性 | |

## 复盘

- **哪个入口的穿透率最高？** 多数系统会发现是检索文档或工具返回，而不是用户输入——这正好印证了间接注入占多数
- 修复里哪一项收益最大？如果是「收工具权限」或「加人确认」，说明确定性防御的优先级判断是对的
- critical 穿透降到 0 了吗？没有的话，剩下的那几条是不是必须靠人确认兜底
- 20 条够吗？有没有你觉得该有但想不出怎么写的攻击类型——那通常说明那个面还没想清楚
- 这套载荷什么时候需要补？记下触发条件：新增数据源、新增工具、新增租户类型

## 关联
- [[01-间接提示注入与 20 条攻击载荷]]
- [[02-纵深防御]]
- [[04-权限过滤时机]]
- [[06-多租户隔离]]
- [[03-对抗测试集]]
