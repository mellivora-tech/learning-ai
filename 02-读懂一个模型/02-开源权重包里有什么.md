---
tags:
  - ai工程/读懂模型
stage: 0.5
status: todo
aliases:
  - safetensors
  - chat template
  - 权重文件
---
`git clone` 一个模型下来，几十个 GB，里面到底是些什么文件，各自负责什么。

## 一句话

权重只是其中一个文件；决定模型「表现正不正常」的往往是旁边那几个几 KB 的配置，尤其是 chat template。

## 原理

### 打开看看

![[权重包里有什么.excalidraw.md|700]]

一个典型的 HuggingFace 模型仓库，去掉杂项就这八样。挨个说。

### 权重本体：safetensors 与分片

```
model-00001-of-00004.safetensors     4.9 GB
model-00002-of-00004.safetensors     5.0 GB
model-00003-of-00004.safetensors     4.9 GB
model-00004-of-00004.safetensors     1.2 GB
model.safetensors.index.json         24 KB
```

大模型不会存成一个巨型文件——不好下载、不好断点续传、不好并行加载，所以切成几 GB 一片。`index.json` 是那本目录，记录每个张量名在第几片：

```json
{
  "weight_map": {
    "model.embed_tokens.weight": "model-00001-of-00004.safetensors",
    "model.layers.0.self_attn.q_proj.weight": "model-00001-of-00004.safetensors",
    "lm_head.weight": "model-00004-of-00004.safetensors"
  }
}
```

> [!danger] 看到 `.bin` 要警惕
> 老仓库里的 `pytorch_model.bin` 是 Python pickle 格式，**加载时会执行文件里的代码**。这不是理论风险，是真出过事的攻击面。safetensors 就是为了解决这个问题而生的：纯数据，没有可执行内容，还能内存映射加载得更快。
> 两种格式都有的话一定选 safetensors；只有 `.bin` 的话，权衡一下这个模型值不值得你冒这个险，相关讨论见 [[04-模型权重来源]]。

### config.json：架构长什么样

```json
{
  "architectures": ["Qwen3ForCausalLM"],
  "hidden_size": 4096,
  "num_hidden_layers": 36,
  "num_attention_heads": 32,
  "num_key_value_heads": 8,
  "head_dim": 128,
  "intermediate_size": 12288,
  "max_position_embeddings": 40960,
  "rope_theta": 1000000,
  "torch_dtype": "bfloat16",
  "vocab_size": 151936
}
```

（Qwen3-8B 的真实配置，一字未改。）

推理引擎靠它知道该按什么形状把权重装进去。几个实用的读法：

- `num_key_value_heads` 比 `num_attention_heads` 小，说明用了 GQA，KV cache 会小一个倍数，见 [[02-KV cache 为什么存在]]
- `max_position_embeddings` 是训练时的上下文长度，超过它就进入外推区，效果不保证。上面那个 40960 是 Qwen3-8B 原生的值，想再长得靠 YaRN 之类的外推手段。这个数字每代都在涨，但它只说明「训到哪」，不代表实测有效长度，见 [[07-位置编码与长度外推]]
- `torch_dtype` 告诉你权重原本是什么精度，据此估文件大小：参数量 × 2 字节

### 分词器：两个文件

`tokenizer.json` 是词表和合并规则，`tokenizer_config.json` 是它的配置。后者里藏着**整个包里最容易出事的东西**：

```jinja
{% for message in messages %}
  {{ '<|im_start|>' + message['role'] + '\n' + message['content'] + '<|im_end|>\n' }}
{% endfor %}
{% if add_generation_prompt %}{{ '<|im_start|>assistant\n' }}{% endif %}
```

Qwen 用的是 ChatML 这套标记（`<|im_start|>` / `<|im_end|>`），Llama 系用的是另一套（`<|start_header_id|>` / `<|eot_id|>`）。**两套完全不通用**——这也是为什么不能自己拼。

这是 **chat template**，一段 Jinja 模板，负责把 `[{"role": "user", "content": "..."}]` 这样的结构拼成模型真正看到的那一长串字符串。

> [!warning] chat template 拼错，模型会「忽然变笨」
> 模型在 SFT 阶段见到的输入全是按这个格式拼的。你拼错一个特殊 token、少一个换行、把 system 消息放错位置，模型看到的就是训练时从没见过的形态，输出质量断崖下跌——但它不会报错，只会答得莫名其妙。
> 排查「模型好像不太行」的时候，**先把实际发给模型的完整字符串打印出来**看一眼，比调三天提示词管用。用 `tokenizer.apply_chat_template(msgs, tokenize=False)` 就能看到。

### generation_config.json：默认怎么生成

```json
{
  "bos_token_id": 151643,
  "eos_token_id": [151645, 151643],
  "temperature": 0.6,
  "top_k": 20,
  "top_p": 0.95
}
```

注意 `eos_token_id` 是个**列表**——Qwen3 有两个停止符（`<|im_end|>` 和 `<|endoftext|>`）。自己写推理循环时只判断其中一个，模型会停不下来，一直往下唠。这不是 Qwen 特有的，Llama 3 也是两个。

这里的采样参数是厂商建议值，不是硬性规定，含义见 [[04-采样参数]]。

> [!warning] 会思考的模型多一层坑
> Qwen3 这类带思考能力的模型，chat template 里还管着 `<think>...</think>` 块怎么拼。开不开思考、思考内容要不要回灌到下一轮，都由模板决定。多轮对话里把上一轮的思考块原样塞回去，通常是错的——先读模板再动手。

### model card：许可证和自测分数

`README.md` 里通常有训练数据的笼统描述、评测分数、以及**许可证**。许可证这一项在商用场景是硬约束：有的禁止商用，有的按月活用户数设门槛，有的要求衍生模型必须署名。别等上线了才看，见 [[04-模型权重来源]]。

上面写的评测分数是厂商自己跑的，怎么读见 [[03-评测基准都在测什么]]。

### 还可能有的

- `*.gguf` —— llama.cpp 用的单文件格式，量化模型常见
- `adapter_model.safetensors` + `adapter_config.json` —— LoRA 权重，只有几十 MB，得配上基座模型才能用，见 [[03-LoRA 与 QLoRA]]
- `quantize_config.json` —— 量化模型的配置，见 [[09-量化]]

## 动手做

- [ ] `huggingface-cli download` 一个 7B 模型，`ls -lh` 看每个文件多大，和上面对一遍
- [ ] 用 `safetensors` 库打开一个分片，列出里面的张量名和形状，和 `config.json` 逐项对上号
- [ ] 用 `tokenizer.apply_chat_template(msgs, tokenize=False)` 打印一段三轮对话拼出来的完整字符串，数一数特殊 token 的位置
- [ ] 故意把 chat template 改坏（少一个 `<|eot_id|>`），跑同一个 prompt，记录输出质量怎么塌
- [ ] 打开 `Qwen/Qwen3-8B` 的 `generation_config.json`，确认 `eos_token_id` 是不是列表，两个值分别对应哪个特殊 token

## 算学会了

- [ ] 能说出权重包里每个文件的作用
- [ ] 排查「模型表现异常」时，第一步是把实际输入字符串打出来看，而不是改提示词
- [ ] 知道为什么不该加载 `.bin`
- [ ] 看到 `num_key_value_heads < num_attention_heads` 能立刻说出这意味着什么
- [ ] 上线前会先看许可证

## 坑

**只关心权重，不看配置。** 权重几十 GB 很显眼，chat template 几行很不起眼，但后者出错的概率高得多，而且症状是「模型变笨」这种最难定位的形态。

**自己拼对话格式。** 手写 `"User: ... Assistant: ..."` 这种，看着合理，但和模型训练时见到的格式差着十万八千里。永远用 `apply_chat_template`。

**只判断一个 eos。** 停止符是列表的模型不少，漏掉一个的表现是模型说完正事继续自言自语，很容易被误诊成「模型话痨」。

**以为 LoRA 权重能单独用。** 那几十 MB 只是增量，得叠在对应的基座上。基座版本对不上，效果会很奇怪。

**下载完就上线，没看许可证。** 这是唯一一个会带来法务后果的坑，而它只需要读两分钟。

## 关联
- [[00-一个模型到底是什么]]
- [[00-模型是怎么来的]]
- [[03-评测基准都在测什么]]
- [[04-采样参数]]
- [[03-Tokenization]]

## 参考

| 年份 | 工作 | 人与机构 | 在本篇的位置 |
| --- | --- | --- | --- |
| — | [Qwen3-8B 的仓库文件列表](https://huggingface.co/Qwen/Qwen3-8B/tree/main) | 阿里巴巴通义千问团队 | **本篇所有配置的出处**，逐个文件对照着读 |
| — | [safetensors](https://github.com/huggingface/safetensors) | Hugging Face | 格式说明，以及为什么要替掉 pickle——展开见 [[04-模型权重来源]] |
| — | [Chat Templates 文档](https://huggingface.co/docs/transformers/chat_templating) | Hugging Face | 模板机制与 `apply_chat_template` 的用法 |
| 2025 | [The 4 Things Qwen-3's Chat Template Teaches Us](https://huggingface.co/blog/qwen-3-chat-template-deep-dive) | Hugging Face 博客 | 把一个真实 chat template 逐行拆开讲，本篇模板那节的延伸 |

**权重包的内容会随模型变**，上面给的是「去哪儿看」。拿到一个新模型，按 `config.json` → `tokenizer_config.json` → `generation_config.json` → 权重分片索引这个顺序读一遍，本篇讲的每一项都能对上号。
