---
tags:
  - ai工程/显卡
  - 里程碑
stage: 0
status: todo
---
## 交付物

一份一页纸的推演，加一次把它验证掉的压测。给定一张卡和一个模型，先**纯靠算**给出结论，再实测打脸或者对上：

- 最大并发数（受显存约束）——写出三笔账各占多少
- 单请求 decode 速度上限（受带宽约束）
- Prefill 吃 8k 输入要多久（受算力约束）
- 上下文开到 4 倍时，上面三个数各变成多少
- 实测值与推算值的比值，以及差距的解释

## 做法

1. 挑一张手上能拿到的卡，抄下四个关键数字：显存、带宽、算力、互联
2. 挑一个模型，从 `config.json` 抄出层数、KV 头数、head_dim、参数量
3. 算显存：权重 + KV cache（按目标上下文和并发）+ 20% 余量，反推最大并发
4. 算带宽：显存带宽 ÷ 每 token 要搬的字节 = decode token/s 上限
5. 算算力：$2N \times T$ FLOP ÷ 卡的算力 = prefill 理论耗时
6. 把上下文乘 4，三个数重算一遍
7. 起服务压测，记实测值
8. 算达成率，解释差距——差在 KV cache 也要读？差在 batch 没堆起来？差在算子没调优？

## 结果与数据

| | 推算 | 实测 | 达成率 |
| --- | --- | --- | --- |
| 最大并发 | | | |
| 单请求 token/s | | | |
| Prefill 8k 耗时 | | | |

## 复盘

- 哪一项推算得最准？哪一项差得最远？差距的物理原因是什么
- 上下文乘 4 之后，卡住的是显存还是带宽
- 如果预算翻倍，加卡、换卡、还是花在量化上，收益更大
- 这张卡上跑这个模型，单位产出成本是多少

## 关联
- [[01-一张卡的四个关键数字]]
- [[02-算力经常用不满：两种瓶颈]]
- [[06-怎么给任务选卡]]
- [[03-KV cache 管理与显存测算]]
