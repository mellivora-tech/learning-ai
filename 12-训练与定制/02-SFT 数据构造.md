---
tags:
  - ai工程/训练定制
stage: 9
status: todo
aliases:
  - SFT
  - 指令微调数据
---
微调的效果几乎全部由数据决定。而多数失败的微调，问题不在超参，在那批数据一开始就不该那么造。

## 一句话

SFT 数据的核心要求是**质量和一致性**，不是数量——一千条标准统一的样本，胜过一万条互相矛盾的。

## 原理

### 格式与 loss mask

SFT 的损失函数和预训练一样是交叉熵，区别只在**只在回答部分算 loss**，见 [[08-预训练、后训练、对齐]]：

```text
<|im_start|>system
你是发票处理助手<|im_end|>              ← 不算 loss
<|im_start|>user
处理这张发票：……<|im_end|>              ← 不算 loss
<|im_start|>assistant
{"invoice_no": "INV-001", ...}<|im_end|> ← 算 loss
```

**这个 mask 必须和你的推理时格式完全一致**，包括 chat template 的特殊 token，见 [[02-开源权重包里有什么]]。训练时用一套格式、推理时用另一套，是最常见也最难查的失败。

### 数量与质量

经验上的量级：

| 目标 | 大致数量 |
| --- | --- |
| 调整输出格式 | 几百到一千 |
| 学一种特定语气/风格 | 一到三千 |
| 掌握一个窄任务的完整流程 | 三千到一万 |
| 覆盖多个任务 | 一万以上 |

**但数量远不如一致性重要。** 有研究表明一千条高质量样本就能显著改变行为。反过来，一万条标准不统一的数据会让模型学到「这个字段有时填有时不填」这种矛盾模式。

**所以第一件事是把标注规范写下来**，而不是先攒量。

### 从哪来

| 来源 | 优点 | 风险 |
| --- | --- | --- |
| **线上真实数据 + 人工修正** | 分布最真 | 要脱敏，要人力 |
| **大模型生成** | 快、便宜 | 会带上大模型的毛病和风格 |
| 人工从零编写 | 质量最高 | 最贵、分布可能偏 |
| 公开数据集 | 免费 | 和你的任务分布不符 |

**推荐组合：线上真实输入 + 大模型生成初稿 + 人工修正。** 输入用真的（保证分布），输出用生成再修（保证效率和质量）。

用大模型生成时要注意：

- **它会把自己的风格带进去**——如果你想要的风格和它不同，得在提示词里明确
- **它倾向于生成「典型」样本**，边缘情况会缺失，要专门补
- **注意许可证**——有些模型的输出禁止用于训练竞品，见 [[04-模型权重来源]]

### 必须专门覆盖的几类

数据集里最容易缺的恰恰是最重要的：

| 类别 | 为什么必须有 |
| --- | --- |
| **拒答样本** | 不然模型学不会说「我不知道」，见 [[06-检索评估两段式]] |
| **边界情况** | 空输入、超长、格式怪异 |
| **失败恢复** | 工具报错之后该怎么做 |
| **多轮** | 只训单轮，多轮表现会退化 |
| **负例的正确处理** | 用户要求做不该做的事时怎么回应 |

**拒答样本最容易漏**。全是「有问必答」的数据训出来的模型，遇到不知道的会编——而这是最危险的失败模式。

### 混一点通用数据

> [!warning] 只用窄任务数据会让通用能力退化
> 在一个窄领域上激进微调，会损害模型的通用能力，见 [[06-灾难性遗忘]]。
>
> 一个便宜的缓解办法：**在训练集里混入 5–10% 的通用指令数据**。成本几乎为零，能明显保住广泛能力。

### 质量检查清单

开训之前逐项过一遍：

- [ ] 去重——完全重复和近似重复都要去
- [ ] 格式一致——所有样本走同一个 chat template
- [ ] 标注一致——抽样人工复核，看标准有没有飘
- [ ] 长度分布合理——没有大量超长或超短的异常样本
- [ ] 没有敏感信息——脱敏做干净了，见 [[01-数据驻留]]
- [ ] 划分了验证集——**而且验证集要和训练集同分布但无重叠**

**最后一条经常出错**：从同一批数据随机切分，如果原始数据里有近似重复，验证集会被污染，分数虚高。

### 数据要版本化

和 golden set 一样，训练数据要进版本控制，见 [[07-数据版本化]]。理由：

- 效果变化要能归因到哪个版本的数据
- 模型和数据的对应关系要能追溯
- 合规上要能说清「这个模型是用什么数据训的」

## 动手做

- [ ] 先写标注规范，再开始攒数据
- [ ] 用「真实输入 + 大模型生成 + 人工修正」的组合造第一批
- [ ] 专门补齐五类容易缺的样本，尤其拒答
- [ ] 混入 5–10% 通用指令数据
- [ ] 过一遍质量检查清单，特别是去重和验证集划分
- [ ] 验证训练格式和推理时的 chat template 完全一致
- [ ] 把数据集版本化进仓库

## 算学会了

- [ ] 知道 loss mask 只作用在回答部分，且格式必须和推理时一致
- [ ] 优先保证一致性而非数量，先写规范再攒数据
- [ ] 数据集里有拒答、边界、失败恢复、多轮四类专门样本
- [ ] 混了通用数据防止能力退化
- [ ] 验证集和训练集无重叠，分数不虚高

## 坑

**训练格式和推理格式不一致。** 最难查的一类失败，模型表现莫名其妙地差。

**先攒量后定标准。** 攒完发现标注不一致，只能重来。

**没有拒答样本。** 训出来的模型遇到不知道的会编。

**验证集被近似重复污染。** 分数虚高，上线打脸。

**全用大模型生成。** 带上它的风格和盲区，边缘情况全缺。

## 关联
- [[01-四条路线选型：prompt、RAG、LoRA、蒸馏]]
- [[03-LoRA 与 QLoRA]]
- [[06-灾难性遗忘]]
- [[05-标注流程]]
- [[07-数据版本化]]

## 参考
- [LIMA: Less Is More for Alignment](https://arxiv.org/abs/2305.11206) — 一千条高质量样本的效果
- [LLM Fine-Tuning Guide 2026](https://futureagi.com/blog/llm-fine-tuning-guide-2025/) — 数据构造与混入通用数据的实践建议
- [InstructGPT](https://arxiv.org/abs/2203.02155) — SFT 数据的原始设计思路
